{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "secondary", "secondary-window-*"],
  "permissions": [
    "core:default",
    "opener:default",
    "drag:default"
  ]
}
